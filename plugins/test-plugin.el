;;; test-plugin.el --- Test plugin for the grease plugin loader -*- lexical-binding: t; -*-

;;; Commentary:
;; Minimal plugin used to verify that `grease-load-plugins' works.
;; It simply prints a message when it is loaded.

;;; Code:

(message "[grease plugin] test-plugin.el loaded!")

;;; test-plugin.el ends here
